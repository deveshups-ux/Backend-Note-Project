<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= fileName %></title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>

  <body
    class="bg-gradient-to-br from-zinc-900 via-zinc-800 to-zinc-900 min-h-screen text-white flex items-center justify-center"
  >
    <div
      class="w-full max-w-3xl bg-zinc-800/60 backdrop-blur-lg shadow-2xl rounded-2xl p-8 border border-zinc-700"
    >
      <form
        action="/edit"
        method="post"
        class="bg-zinc-800 max-w-md mx-auto p-8 rounded-2xl shadow-xl flex flex-col gap-6 mb-12"
      >
        <h2 class="text-2xl font-semibold text-center">Create Task</h2>

        <input
          class="w-full px-4 py-3 rounded-lg bg-zinc-500 text-zinc-700 text-white outline-none focus:ring-2 focus:ring-blue-500 transition"
          type="text"
          name="previous"
          value="<%=  fileName %>"
          placeholder="Previous Name"
        />
        <input
          class="w-full px-4 py-3 rounded-lg bg-zinc-700 text-white outline-none focus:ring-2 focus:ring-blue-500 transition"
          type="text"
          name="new"
          placeholder="New Name"
        />

        <button
          class="bg-yellow-600 hover:bg-blue-700 active:scale-95 transition-all duration-200 py-3 rounded-lg font-medium"
          type="submit"
        >
          Update Name
        </button>
      </form>
    </div>
  </body>
</html>
