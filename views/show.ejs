<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= fileName %></title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>

  <body
    class="bg-gradient-to-br from-zinc-900 via-zinc-800 to-zinc-900 min-h-screen text-white flex items-center justify-center"
  >
    <div
      class="w-full max-w-3xl bg-zinc-800/60 backdrop-blur-lg shadow-2xl rounded-2xl p-8 border border-zinc-700"
    >
      <!-- Back Button -->
      <a
        href="/"
        class="inline-block mb-6 text-sm text-blue-400 hover:text-blue-300 transition duration-200"
      >
        ← Back To Home
      </a>

      <!-- File Name -->
      <h1 class="text-3xl font-bold mb-4 text-blue-400 break-words">
        <%= fileName %>
      </h1>

      <!-- Divider -->
      <div class="h-px bg-zinc-700 mb-6"></div>

      <!-- File Content -->
      <div
        class="bg-zinc-900 p-6 rounded-xl border border-zinc-700 shadow-inner"
      >
        <p class="text-zinc-300 leading-relaxed whitespace-pre-line">
          <%= fileData %>
        </p>
      </div>
    </div>
  </body>
</html>
